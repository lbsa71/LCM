# Local Open WebUI / API compatibility settings. Copy to `.env` and replace
# values locally; never commit real credentials.
OPENAI_API_BASE_URLS=http://localhost:8000/v1
OPENAI_API_KEYS=local-development-only
DEFAULT_MODELS=lcm-local
